CSS Expression Grammar
======================

This grammar describes the expression subset currently implemented by the
values parser. Whitespace may occur between any two components unless stated
otherwise.

expression
    ::= additive-expression

additive-expression
    ::= multiplicative-expression
        ((PLUS | MINUS) multiplicative-expression)*

multiplicative-expression
    ::= primary-expression
        ((STAR | SLASH) primary-expression)*

primary-expression
    ::= NUMBER
      | PERCENTAGE
      | DIMENSION
      | parenthesized-expression
      | math-function

parenthesized-expression
    ::= PAREN_OPEN expression PAREN_CLOSE

math-function
    ::= MIN_FUNCTION
      | MAX_FUNCTION
      | CLAMP_FUNCTION

MIN_FUNCTION
    ::= "min(" expression (COMMA expression)+ ")"

MAX_FUNCTION
    ::= "max(" expression (COMMA expression)+ ")"

CLAMP_FUNCTION
    ::= "clamp(" expression COMMA expression COMMA expression ")"


Operator precedence
-------------------

1. Parenthesized expressions
2. Multiplication and division
3. Addition and subtraction

Operators at the same precedence level associate from left to right.


Supported value forms
---------------------

- Numeric literals
- Percentages
- Dimensions with units
- Parenthesized expressions
- min(), max(), and clamp() with numeric arguments
- The operators +, -, *, and /
- var() substitution before expression parsing

Other nested functions remain outside this grammar. `var()` is resolved before
expression parsing and is therefore not an expression primary here.
